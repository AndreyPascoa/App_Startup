import { Text, View } from "react-native";

export function Config() {
    return (
        <View style={{backgroundColor: '#fff', alignItems: 'center', justifyContent: 'center'}}>
            <Text>Tela de Config do Usuario</Text>
        </View>
    )
}